import { css } from '@emotion/core';

export const mainContainerClass = css({
  flex: 1,
});

export const landingContainerClass = css({
  display: 'flex',
  minHeight: '100vh',
  flexDirection: 'column',
});

export const centerClass = css({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  flexDirection: 'column',
});

export const headerContainerClass = css({
  backgroundColor: '#aba8cf',
  color: '#ffffff',
});

export const footerContainerClass = css({
  backgroundColor: '#aba8cf',
  color: '#ffffff',
});
